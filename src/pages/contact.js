import React from 'react';
import { Layout } from '../components/layout';
import { Header } from '../components/header';

export default () => {
  return (
    <Layout>
      <Header />
      <h1>Contact</h1>
    </Layout>
  );
};
